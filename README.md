
Ausgabefunktionen:
    - player_entry()
    - display_current_player(player)
    - display_board(board)
    - display_result(is_win, winner)
    - get_choice()
    - clear_screen()
    - display_error(message)
    - play_again()

Spielfunktionen:
    - generate_board()
    - insert_circle(board, column, color)
    - check_win(board)
    - check_column(column)